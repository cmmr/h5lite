% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.r
\name{h5_read_all}
\alias{h5_read_all}
\title{Read an HDF5 Group or Dataset Recursively}
\usage{
h5_read_all(file, name, attrs = TRUE)
}
\arguments{
\item{file}{Path to the HDF5 file.}

\item{name}{Name of the group or dataset to read (e.g., "/data").}

\item{attrs}{Controls which HDF5 attributes are read and attached to the returned R object(s).
Defaults to \code{TRUE}. See \code{\link[=h5_read]{h5_read()}} for more details.}
}
\value{
A nested \code{list} representing the HDF5 group structure, or a single
R object if \code{name} points to a dataset.
}
\description{
Reads an HDF5 group and all its contents (subgroups and datasets) into a
nested R list. If the target \code{name} is a dataset, it is read directly.
}
\details{
When reading a group, the elements in the returned list are sorted
alphabetically by name, which may differ from their original creation order.
}
\examples{
file <- tempfile(fileext = ".h5")

# Create a nested structure
h5_write(file, "/config/version", 1.2)
h5_write(file, "/data/matrix", matrix(1:4, 2, 2))
h5_write(file, "/data/vector", 1:10)

# Read the entire 'data' group
data_group <- h5_read_all(file, "data")
str(data_group)

# Read the entire file from the root
all_content <- h5_read_all(file, "/")
str(all_content)

unlink(file)
}
\seealso{
\code{\link[=h5_read]{h5_read()}}, \code{\link[=h5_write_all]{h5_write_all()}}, \code{\link[=h5_is_group]{h5_is_group()}}
}
