# h5lite 2.1.0.0

* Added support for SZIP compression - see `vignette('data-compression')`.
* Added support for partial reading - see `vignette('partial-reading')`.
* `h5_length()` and `h5_dim()` now return numeric values instead of integer.



# h5lite 2.0.0.2

* Initial CRAN submission.
