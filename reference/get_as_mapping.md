# Sanity check the 'as' argument Ensures all values are valid and that multiple values are named.

Sanity check the 'as' argument Ensures all values are valid and that
multiple values are named.

## Usage

``` r
get_as_mapping(as, choices)
```
